import {
  spacing,
  colors,
} from '../../styles/designTokens';

export const utils = {
  focus: {
    transform: `box-shadow .3s ease-in-out`,
    shadow: `0 0 0 ${spacing.spacer4} ${colors.primaryDark}`,
  }
};